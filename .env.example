# Kopiere diese Datei nach `.env` und trage dort echte Keys ein.
# Diese Example-Datei sollte niemals echte Secrets enthalten.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=

# Ohne API-Kontingent: simulierte Antworten (nur Dev/UI). Dann sind die Keys optional.
# VITE_AIDIFF_MOCK=true