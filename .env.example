# Copy to `.env` and add real keys. Never commit secrets.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=

# Optional UI locale (must exist in src/i18n/catalog.js), e.g. en
# VITE_AIDIFF_LOCALE=en
